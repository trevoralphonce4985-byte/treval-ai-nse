fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
requests==2.32.5
loguru==0.7.3
numpy>=1.26.0  # Use '>=1.26.0' instead of a specific version, or find a specific version compatible with 3.14.3
scipy>=1.13.0  # Similarly, use '>=' or find a specific compatible version
pandas>=2.2.0  # Similarly, use '>=' or find a specific compatible version
# statsmodels>=0.14.0 # Add this later when needed, ensure compatibility
# arch>=7.0.0       # Add this later when needed, ensure compatibility
# scikit-learn>=1.5.0 # Add this later when needed, ensure compatibility
